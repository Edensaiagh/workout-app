# Changelog

## 2026-09-13 — Firebase auth flow, exercise picker, and workout tracker improvements

**התחברות וחשבון**
- מסך התחברות/הרשמה חדש (`LoginScreen`) עם אימייל+סיסמה, ותשתית לכניסה עם Google (דורש development build, לא Expo Go).
- מסך חשבון חדש (`AccountScreen`) עם התנתקות.
- `authContext` חדש שעוטף את האפליקציה ב-`AuthProvider`, ומציג מסך טעינה עד שברור אם יש משתמש מחובר.
- `App.tsx` הוחלף לניווט בטאבים (אימון / היסטוריה / חשבון) עם ערכת נושא כהה, ומציג את `LoginScreen` כשאין משתמש מחובר.

**סנכרון אימונים לענן**
- אימונים שנכשלו בשמירה לענן (בגלל נפילת רשת) נשמרים מקומית ומנסים להסתנכרן אוטומטית בפעם הבאה שיש משתמש מחובר (`syncPendingWorkoutsToCloud`, נקרא בעליית האפליקציה).
- נוספה `deleteWorkout` שמוחקת גם מ-Firestore וגם מהגיבוי המקומי, כדי שאימון שנמחק לא ינסה להסתנכרן מחדש.
- שאילתת `getUserWorkouts` הוסרה ממנה ה-`orderBy` בצד השרת (נמנע צורך באינדקס מורכב ב-Firebase) והמיון עבר לצד הלקוח.

**מעקב אימון (WorkoutTrackerScreen) ומסך היסטוריה**
- בורר תרגילים חדש (`ExerciseNamePicker`) עם רשימת תרגילים נפוצים (`commonExercises`).
- אפשרות למחוק סט בודד מתוך תרגיל (`deleteSet` בסטור).
- זמן מנוחה בין תרגילים כעת מתחיל אוטומטית גם בסיום התרגיל האחרון, ואפשר להאריך אותו במספר שניות משתנה (במקום קבוע של 10 שניות).
- מסך ההיסטוריה הורחב משמעותית (מ-~ עשרות שורות ל-400+ שורות) - כנראה תצוגת פירוט אימונים ועריכה/מחיקה.

**תיקון תזמון צלילי מנוחה**
- תוקן דיליי בין הצליל למספר על המסך: קודם `seekTo(0)` רץ באופן סינכרוני לפני כל `play()`, מה שהוסיף נסיעה כפולה דרך ה-bridge בדיוק ברגע הקריטי. כעת האיפוס לתחילת הקובץ קורה רק אחרי `didJustFinish` בפועל.

**קבצים שהשתנו:** 16 קבצים, 2070+ שורות נוספו, 193 הוסרו.
**קומיט:** [`90ff2c8`](https://github.com/Edensaiagh/workout-app/commit/90ff2c83199d2f80bd2f5be3d32fa055af7933ca)
